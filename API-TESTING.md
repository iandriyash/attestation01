markdown# Тестирование API приложения "Пиццерия"

## Содержание
- [Запуск приложения](#запуск-приложения)
- [Авторизация](#авторизация)
- [Тестирование через Postman](#тестирование-через-postman)
- [Тестирование через Swagger UI](#тестирование-через-swagger-ui)
- [Примеры всех запросов](#примеры-всех-запросов)
- [Возможные ошибки и решения](#возможные-ошибки-и-решения)

---

## Запуск приложения

Перед тестированием убедитесь, что приложение запущено:
```bash
cd docker
docker-compose up --build
```

Подождите, пока в логах появится:
```
Started PizzeriaApplication in X seconds
```

**Приложение доступно на:** `http://localhost:8080`

---

## 🔐 Авторизация

### Важная информация

API эндпоинты `/api/**` защищены Basic Auth авторизацией.

**Учётные данные для тестирования:**

| Username | Password | Роль | Для чего использовать |
|----------|----------|------|----------------------|
| `user` | `user123` | USER | Обычное тестирование |
| `admin` | `admin123` | ADMIN | Административные операции |

**Для всех примеров в инструкции используется:** `user` / `user123`

---

### Что требует авторизацию?

**🔒 Требуют пароль:**
- Все запросы к `/api/**` (GET, POST, PATCH, DELETE)
- Выполнение запросов через Swagger UI (кнопка "Execute")
- Все запросы в Postman

**✅ НЕ требуют пароль:**
- Открытие Swagger UI в браузере (`http://localhost:8080/swagger-ui/index.html`)
- Просмотр документации API
- Главная страница (`http://localhost:8080/`)

---

## Тестирование через Postman

### Шаг 1: Установка Postman (если не установлен)

1. Скачайте Postman: https://www.postman.com/downloads/
2. Установите и запустите

---

### Шаг 2: Создание коллекции

#### 2.1. Создать новую коллекцию:

1. Откройте Postman
2. Слева найдите раздел **"Collections"**
3. Нажмите кнопку **"+"** (или **"New"** → **"Collection"**)
4. В поле **Name** введите: `Pizzeria (finalProject)`
5. Нажмите **"Create"**

---

#### 2.2. Настройка авторизации для коллекции (ОБЯЗАТЕЛЬНО):

⚠️ **Внимание:** Без этого шага все запросы вернут ошибку **401 Unauthorized**!

**Шаги настройки:**

1. Кликните **правой кнопкой** на коллекцию **Pizzeria (finalProject)**
2. Выберите **"Edit"** (или значок "...")
3. Перейдите на вкладку **"Authorization"**
4. В выпадающем списке **Type** выберите: **"Basic Auth"**
5. Заполните поля:
    - **Username:** `user`
    - **Password:** `user123`
6. Нажмите **"Save"**

✅ **Готово!** Теперь все запросы в этой коллекции автоматически используют авторизацию.

---

### Шаг 3: Создание запросов

---

#### ✅ ЗАПРОС 1: GET /api/pizzas (Получить список пицц)

**3.1. Создать новый запрос:**

1. Кликните **правой кнопкой** на коллекцию **Pizzeria (finalProject)**
2. Выберите **"Add request"**
3. В поле **Request name** введите: `GET /api/pizzas`
4. Нажмите **"Save"** (или просто Enter)

---

**3.2. Настроить запрос:**

1. В выпадающем списке слева выберите метод: **GET**
2. В строке URL введите:
```
   http://localhost:8080/api/pizzas
```
3. Нажмите синюю кнопку **"Send"**

---

**3.3. Проверить результат:**

**Статус:** должен быть `200 OK` (зелёный текст)

**Response body** (внизу): JSON массив с пиццами:
```json
[
  {
    "id": 1,
    "name": "Маргарита",
    "description": "Томатный соус, моцарелла, базилик",
    "price": 450.00,
    "createdAt": "2025-11-20T..."
  },
  {
    "id": 2,
    "name": "Пепперони",
    "description": "Томатный соус, моцарелла, пепперони",
    "price": 550.00,
    "createdAt": "2025-11-20T..."
  },
  {
    "id": 3,
    "name": "Четыре сыра",
    "description": "Моцарелла, пармезан, горгонзола, фета",
    "price": 650.00,
    "createdAt": "2025-11-20T..."
  },
  {
    "id": 4,
    "name": "Мексиканская",
    "description": "Томатный соус, моцарелла, говядина, перец халапеньо",
    "price": 690.00,
    "createdAt": "2025-11-20T..."
  }
]
```

✅ **Если видите это - запрос работает!**

---

#### ✅ ЗАПРОС 2: POST /api/orders (Создать заказ)

**3.4. Создать новый запрос:**

1. Кликните **правой кнопкой** на коллекцию **Pizzeria (finalProject)**
2. Выберите **"Add request"**
3. Введите имя: `POST /api/orders`
4. Нажмите **"Save"**

---

**3.5. Настроить запрос:**

**ШАГ A: Установить метод и URL**

1. В выпадающем списке выберите: **POST**
2. В строке URL введите:
```
   http://localhost:8080/api/orders
```

---

**ШАГ B: Настроить заголовки (Headers)**

1. Нажмите на вкладку **"Headers"** (под строкой URL)
2. Проверьте, что есть заголовок:
    - **Key:** `Content-Type`
    - **Value:** `application/json`
    - Галочка **включена** ✅
3. Если такого заголовка нет - добавьте его вручную

---

**ШАГ C: Добавить тело запроса (Body)**

1. Нажмите на вкладку **"Body"** (рядом с Headers)
2. Выберите радио-кнопку **"raw"** ← **ВАЖНО!**
3. Справа в выпадающем списке выберите **"JSON"** (вместо Text) ← **ВАЖНО!**
4. В текстовом поле вставьте JSON:
```json
{
  "customerName": "Иван Иванов",
  "customerPhone": "+79991234567",
  "items": [
    {
      "pizzaId": 1,
      "quantity": 2,
      "price": 450.00
    },
    {
      "pizzaId": 2,
      "quantity": 1,
      "price": 550.00
    }
  ]
}
```

---

**ШАГ D: Отправить запрос**

5. Нажмите синюю кнопку **"Send"**
6. Подождите 1-2 секунды

---

**3.6. Проверить результат:**

**Статус:** должен быть `201 Created` (зелёный текст)

**Response body:** созданный заказ с автоматически рассчитанной суммой:
```json
{
  "id": 1,
  "customerName": "Иван Иванов",
  "customerPhone": "+79991234567",
  "totalPrice": 1450.00,
  "status": "NEW",
  "createdAt": "2025-11-20T20:30:00.123456",
  "items": [
    {
      "pizzaId": 1,
      "quantity": 2,
      "price": 450.00
    },
    {
      "pizzaId": 2,
      "quantity": 1,
      "price": 550.00
    }
  ]
}
```

**Обратите внимание:**
- ✅ `totalPrice` = 2×450 + 1×550 = **1450.00** (рассчитан автоматически)
- ✅ Заказу присвоен уникальный `id`
- ✅ Статус установлен в `NEW`
- ✅ Добавлено время создания `createdAt`

---

#### ✅ ЗАПРОС 3: GET /api/orders (Получить список заказов)

**3.7. Создать запрос:**

1. Правой кнопкой на коллекцию → **"Add request"**
2. Имя: `GET /api/orders`
3. Метод: **GET**
4. URL:
```
   http://localhost:8080/api/orders
```
5. Нажмите **"Send"**

---

**3.8. Проверить результат:**

**Статус:** `200 OK`

**Response:** массив с созданными заказами
```json
[
  {
    "id": 1,
    "customerName": "Иван Иванов",
    "customerPhone": "+79991234567",
    "totalPrice": 1450.00,
    "status": "NEW",
    "createdAt": "2025-11-20T20:30:00.123456",
    "items": [...]
  }
]
```

---

#### ✅ ЗАПРОС 4: GET /api/orders/{id} (Получить заказ по ID)

**3.9. Создать запрос:**

1. Правой кнопкой на коллекцию → **"Add request"**
2. Имя: `GET /api/orders/{id}`
3. Метод: **GET**
4. URL:
```
   http://localhost:8080/api/orders/1
```
*(замените `1` на реальный ID из предыдущего запроса)*
5. Нажмите **"Send"**

---

**3.10. Проверить результат:**

**Статус:** `200 OK`

**Response:** один заказ с полными данными и списком позиций

---

#### ✅ ЗАПРОС 5: PATCH /api/orders/{id}/status (Обновить статус заказа)

**3.11. Создать запрос:**

1. Правой кнопкой на коллекцию → **"Add request"**
2. Имя: `PATCH /api/orders/{id}/status`
3. Метод: **PATCH**
4. URL:
```
   http://localhost:8080/api/orders/1/status
```

---

**3.12. Настроить Body:**

1. Вкладка **"Body"**
2. Выберите **"raw"**
3. Выберите **"JSON"**
4. Вставьте:
```json
{
  "status": "IN_PROGRESS"
}
```

5. Нажмите **"Send"**

---

**3.13. Проверить результат:**

**Статус:** `200 OK`

**Response:** заказ с обновлённым статусом `IN_PROGRESS`

**Доступные статусы для изменения:**
- `NEW` - новый заказ
- `IN_PROGRESS` - в процессе приготовления
- `DELIVERED` - доставлен
- `CANCELLED` - отменён

---

#### ✅ ЗАПРОС 6: GET /api/orders с фильтрами

**3.14. Фильтрация по статусу:**

**Вариант A: Через Query Params (рекомендуется)**

1. Создайте запрос: `GET /api/orders (filtered)`
2. URL: `http://localhost:8080/api/orders`
3. Перейдите на вкладку **"Params"** (рядом с Authorization, Headers)
4. Добавьте параметр:
    - **Key:** `status`
    - **Value:** `NEW`
    - Галочка **включена** ✅
5. Нажмите **"Send"**

**Результат:** только заказы со статусом `NEW`

---

**Вариант B: Прямо в URL**
```
http://localhost:8080/api/orders?status=NEW
```

---

**3.15. Фильтрация по телефону:**

В вкладке **"Params"** добавьте:
- **Key:** `phone`
- **Value:** `+79991234567`

Нажмите **"Send"**

**Результат:** заказы конкретного клиента

---

**3.16. Комбинированная фильтрация:**

Добавьте **оба параметра**:
- `status` = `NEW`
- `phone` = `+79991234567`

**Результат:** новые заказы конкретного клиента

---

#### ✅ ЗАПРОС 7: DELETE /api/orders/{id} (Soft delete заказа)

**3.17. Создать запрос:**

1. Имя: `DELETE /api/orders/{id}`
2. Метод: **DELETE**
3. URL:
```
   http://localhost:8080/api/orders/1
```
4. Нажмите **"Send"**

---

**3.18. Проверить результат:**

**Статус:** `204 No Content`

**Response body:** пустой (это нормально для 204)

---

**Проверка soft delete:**

Выполните `GET /api/orders` - удалённый заказ не появится в списке.

**Что произошло:**
- Заказ **НЕ удалён** физически из БД
- Установлено поле `is_deleted = true`
- Заказ больше не возвращается в запросах

---

### Шаг 4: Сохранение коллекции

**4.1. Проверить, что все запросы сохранены:**

- Убедитесь, что рядом с именами запросов нет значка `*` (звёздочка = несохранённые изменения)
- Если есть - нажмите **Ctrl+S** (Windows) или **Cmd+S** (Mac)

---

**4.2. Экспортировать коллекцию (опционально):**

1. Правой кнопкой на коллекцию **Pizzeria (finalProject)**
2. Выберите **"Export"**
3. Выберите **"Collection v2.1"** (рекомендуется)
4. Нажмите **"Export"**
5. Сохраните файл в удобное место

---

### 📊 Таблица всех запросов

| Метод | Эндпоинт | Body | Ожидаемый результат |
|-------|----------|------|---------------------|
| GET | `/api/pizzas` | - | 200 OK, массив из 4 пицц |
| GET | `/api/pizzas/{id}` | - | 200 OK, одна пицца |
| POST | `/api/pizzas` | JSON пиццы | 201 Created |
| PUT | `/api/pizzas/{id}` | JSON обновления | 200 OK |
| DELETE | `/api/pizzas/{id}` | - | 204 No Content |
| GET | `/api/orders` | - | 200 OK, массив заказов |
| GET | `/api/orders?status=NEW` | - | 200 OK, фильтр |
| GET | `/api/orders?phone=...` | - | 200 OK, фильтр |
| GET | `/api/orders/{id}` | - | 200 OK, один заказ |
| POST | `/api/orders` | JSON заказа | 201 Created |
| PATCH | `/api/orders/{id}/status` | JSON статуса | 200 OK |
| DELETE | `/api/orders/{id}` | - | 204 No Content |

---

## Тестирование через Swagger UI

### Шаг 1: Открыть Swagger UI

1. Убедитесь, что приложение запущено (`docker-compose up`)
2. Откройте браузер
3. Перейдите по адресу:
```
   http://localhost:8080/swagger-ui/index.html
```

---

**Что вы увидите:**

✅ **Страница откроется БЕЗ запроса пароля!**

- Список всех API эндпоинтов
- Разделы: **Pizzas**, **Orders**, **Home**
- Документация по каждому эндпоинту
- Схемы данных (DTO) внизу страницы

> **Примечание:** Swagger UI (сама страница документации) доступна всем БЕЗ пароля.
> Но для **выполнения запросов** (кнопка "Execute") потребуется авторизация!

---

### Шаг 2: Авторизация для выполнения запросов

⚠️ **ОБЯЗАТЕЛЬНЫЙ ШАГ!** Без него все запросы вернут ошибку **401 Unauthorized**.

---

**2.1. Найти кнопку авторизации:**

1. В правом верхнем углу страницы найдите кнопку **"Authorize"**
2. Рядом с ней иконка замка 🔒
3. Нажмите на эту кнопку

---

**2.2. Ввести учётные данные:**

Откроется окно **"Available authorizations"**:

1. Найдите раздел **"basicAuth (http, Basic)"**
2. В поле **username** введите: `user`
3. В поле **password** введите: `user123`
4. Нажмите кнопку **"Authorize"** (внизу окна)
5. Нажмите **"Close"**

---

**2.3. Проверка:**

- Замки 🔒 возле эндпоинтов должны стать **открытыми** (разблокированными)
- Теперь можно выполнять запросы через "Execute"!

✅ **Авторизация выполнена!**

---

### Шаг 3: Тестирование эндпоинтов

---

#### 🟢 Тест 1: GET /api/pizzas (Получить список пицц)

**3.1. Найти эндпоинт:**

1. Найдите раздел **"Pizzas"** (Операции с пиццами)
2. Найдите эндпоинт **GET /api/pizzas** (голубая кнопка)
3. Кликните на него - раздел раскроется

---

**3.2. Выполнить запрос:**

1. Нажмите кнопку **"Try it out"** (справа вверху)
2. Нажмите синюю кнопку **"Execute"** (внизу)
3. Прокрутите вниз до раздела **"Server response"**

---

**3.3. Проверить результат:**

**Code:** `200` (зелёный текст)

**Response body:** JSON массив с 4 пиццами:
```json
[
  {
    "id": 1,
    "name": "Маргарита",
    "description": "Томатный соус, моцарелла, базилик",
    "price": 450.00,
    "createdAt": "2025-11-20T..."
  },
  {
    "id": 2,
    "name": "Пепперони",
    "description": "Томатный соус, моцарелла, пепперони",
    "price": 550.00,
    "createdAt": "2025-11-20T..."
  },
  ...
]
```

✅ **Если видите код 200 и список пицц - всё работает!**

---

#### 🟢 Тест 2: POST /api/orders (Создать заказ)

**3.4. Найти эндпоинт:**

1. Найдите раздел **"Orders"** (Операции с заказами)
2. Найдите **POST /api/orders** (зелёная кнопка)
3. Кликните на него

---

**3.5. Подготовить данные:**

1. Нажмите **"Try it out"**
2. В поле **"Request body"** вы увидите шаблон JSON
3. **УДАЛИТЕ** весь шаблон (выделите всё и Delete)
4. **ВСТАВЬТЕ** следующий JSON:
```json
{
  "customerName": "Анна Петрова",
  "customerPhone": "+79991112233",
  "items": [
    {
      "pizzaId": 1,
      "quantity": 3,
      "price": 450.00
    },
    {
      "pizzaId": 4,
      "quantity": 1,
      "price": 690.00
    }
  ]
}
```

---

**3.6. Выполнить запрос:**

1. Нажмите синюю кнопку **"Execute"**
2. Подождите 1-2 секунды
3. Прокрутите вниз до **"Server response"**

---

**3.7. Проверить результат:**

**Code:** `201` (зелёный) - "Заказ создан"

**Response body:**
```json
{
  "id": 2,
  "customerName": "Анна Петрова",
  "customerPhone": "+79991112233",
  "totalPrice": 2040.00,
  "status": "NEW",
  "createdAt": "2025-11-20T21:25:19.197227",
  "items": [
    {
      "pizzaId": 1,
      "quantity": 3,
      "price": 450.00
    },
    {
      "pizzaId": 4,
      "quantity": 1,
      "price": 690.00
    }
  ]
}
```

**Обратите внимание:**
- ✅ `totalPrice` = 3×450 + 1×690 = **2040.00** (рассчитан автоматически!)
- ✅ Присвоен уникальный `id: 2`
- ✅ Статус: `NEW`
- ✅ Добавлено время создания

---

#### 🟢 Тест 3: GET /api/orders (Получить список заказов)

**3.8. Найти и раскрыть:**

1. В разделе **"Orders"**
2. Найдите **GET /api/orders** (голубая кнопка)
3. Кликните

---

**3.9. Выполнить без фильтров:**

1. Нажмите **"Try it out"**
2. **Оставьте все поля пустыми:**
    - `status` - пусто
    - `phone` - пусто
3. Нажмите **"Execute"**

---

**3.10. Проверить результат:**

**Code:** `200`

**Response:** массив всех заказов (включая созданные ранее)
```json
[
  {
    "id": 1,
    "customerName": "Иван Иванов",
    ...
  },
  {
    "id": 2,
    "customerName": "Анна Петрова",
    ...
  }
]
```

---

#### 🟢 Тест 4: GET /api/orders с фильтром по статусу

**3.11. Тот же эндпоинт, но с фильтром:**

1. Убедитесь, что **"Try it out"** активен
2. В поле **status** введите: `NEW`
3. Поле **phone** оставьте пустым
4. Нажмите **"Execute"**

---

**3.12. Проверить результат:**

**Code:** `200`

**Response:** только заказы со статусом `NEW`

---

#### 🟢 Тест 5: PATCH /api/orders/{id}/status (Обновить статус)

**3.13. Найти эндпоинт:**

1. В разделе **"Orders"**
2. Найдите **PATCH /api/orders/{id}/status** (оранжевая кнопка)
3. Кликните

---

**3.14. Подготовить данные:**

1. Нажмите **"Try it out"**
2. В поле **id** введите: `2` (или другой существующий ID)
3. В **Request body** удалите шаблон и вставьте:
```json
{
  "status": "IN_PROGRESS"
}
```

4. Нажмите **"Execute"**

---

**3.15. Проверить результат:**

**Code:** `200`

**Response:** заказ с обновлённым `"status": "IN_PROGRESS"`

---

#### 🟢 Тест 6: GET /api/pizzas/{id} (Получить пиццу по ID)

**3.16. Найти эндпоинт:**

1. В разделе **"Pizzas"**
2. Найдите **GET /api/pizzas/{id}** (голубая кнопка)
3. Кликните

---

**3.17. Выполнить:**

1. Нажмите **"Try it out"**
2. В поле **id** введите: `1`
3. Нажмите **"Execute"**

---

**3.18. Проверить результат:**

**Code:** `200`

**Response:** одна пицца с id=1
```json
{
  "id": 1,
  "name": "Маргарита",
  "description": "Томатный соус, моцарелла, базилик",
  "price": 450.00,
  "createdAt": "2025-11-20T..."
}
```

---

### Шаг 4: Просмотр схем данных

**4.1. Найти раздел Schemas:**

1. Прокрутите страницу Swagger в **самый низ**
2. Найдите раздел **"Schemas"**
3. Раскройте его (кликните)

---

**4.2. Что там находится:**

- `CreateOrderRequest` - структура для создания заказа (какие поля нужны)
- `OrderDto` - структура ответа с заказом
- `PizzaDto` - структура пиццы
- `OrderItemDto` - структура позиции заказа

**Это полезно для понимания:**
- Какие поля обязательны
- Какой тип данных у каждого поля
- Какую структуру ожидает API

---

## Примеры всех запросов

### 📋 Сценарий 1: Полный цикл создания и обработки заказа
```bash
# Шаг 1: Посмотреть меню пицц
GET http://localhost:8080/api/pizzas
Authorization: Basic user:user123

# Ответ: массив из 4 пицц с ID и ценами

---

# Шаг 2: Создать заказ
POST http://localhost:8080/api/orders
Authorization: Basic user:user123
Content-Type: application/json

{
  "customerName": "Дмитрий Смирнов",
  "customerPhone": "+79995554433",
  "items": [
    {"pizzaId": 1, "quantity": 1, "price": 450.00},
    {"pizzaId": 2, "quantity": 1, "price": 550.00},
    {"pizzaId": 3, "quantity": 1, "price": 650.00},
    {"pizzaId": 4, "quantity": 1, "price": 690.00}
  ]
}

# Ответ: 201 Created
# totalPrice = 450 + 550 + 650 + 690 = 2340.00

---

# Шаг 3: Проверить созданный заказ
GET http://localhost:8080/api/orders/3
Authorization: Basic user:user123

# Ответ: 200 OK, заказ с полными данными

---

# Шаг 4: Обновить статус (начали готовить)
PATCH http://localhost:8080/api/orders/3/status
Authorization: Basic user:user123
Content-Type: application/json

{"status": "IN_PROGRESS"}

# Ответ: 200 OK

---

# Шаг 5: Обновить статус (доставили)
PATCH http://localhost:8080/api/orders/3/status
Authorization: Basic user:user123
Content-Type: application/json

{"status": "DELIVERED"}

# Ответ: 200 OK
```

---

### 📋 Сценарий 2: Работа с фильтрами
```bash
# Все новые заказы
GET http://localhost:8080/api/orders?status=NEW
Authorization: Basic user:user123

# Заказы конкретного клиента
GET http://localhost:8080/api/orders?phone=%2B79991234567
Authorization: Basic user:user123

# Новые заказы конкретного клиента (комбинация фильтров)
GET http://localhost:8080/api/orders?status=NEW&phone=%2B79991234567
Authorization: Basic user:user123

# Все заказы в процессе
GET http://localhost:8080/api/orders?status=IN_PROGRESS
Authorization: Basic user:user123

# Все доставленные заказы
GET http://localhost:8080/api/orders?status=DELIVERED
Authorization: Basic user:user123
```

---

## Возможные ошибки и решения

### ⚠️ Ошибка 1: 401 Unauthorized

**Что означает:**
API требует авторизацию, но логин/пароль не указан или неверный.

---

**Причины:**
- ❌ В Postman не настроена авторизация для коллекции (пропущен Шаг 2.2)
- ❌ В Swagger не нажата кнопка "Authorize" (пропущен Шаг 2)
- ❌ Неправильный логин или пароль
- ❌ В Postman тип авторизации не "Basic Auth"

---

**Решение для Postman:**

1. Правой кнопкой на коллекцию **Pizzeria (finalProject)**
2. Выберите **"Edit"**
3. Вкладка **"Authorization"**
4. Type: выберите **"Basic Auth"**
5. Username: `user`
6. Password: `user123`
7. Нажмите **"Save"** ← **ОБЯЗАТЕЛЬНО!**
8. Повторите запрос

---

**Решение для Swagger:**

1. Найдите кнопку **"Authorize"** вверху справа (иконка 🔒)
2. Нажмите на неё
3. Username: `user`
4. Password: `user123`
5. Нажмите **"Authorize"**
6. Нажмите **"Close"**
7. Повторите запрос (Try it out → Execute)

---

**Проверка авторизации через curl:**
```bash
# БЕЗ авторизации (вернёт 401)
curl http://localhost:8080/api/pizzas

# С авторизацией (вернёт 200 и данные)
curl -u user:user123 http://localhost:8080/api/pizzas
```

---

### ⚠️ Ошибка 2: 400 Bad Request

**Что означает:**
Сервер не может обработать запрос из-за неправильного формата данных.

---

**Причины:**
- ❌ Неправильный формат JSON (пропущена запятая, кавычка, скобка)
- ❌ Отсутствуют обязательные поля (`customerName`, `customerPhone`, `items`)
- ❌ Невалидный номер телефона (должен начинаться с `+7` или `+3`)
- ❌ Неправильный тип данных (например, строка вместо числа)

---

**Решение:**

1. **Проверьте JSON через валидатор:**
    - Откройте: https://jsonlint.com/
    - Вставьте ваш JSON
    - Нажмите "Validate JSON"
    - Исправьте найденные ошибки

2. **Проверьте обязательные поля:**
    - `customerName` - должно быть заполнено (строка)
    - `customerPhone` - формат: `+79991234567`
    - `items` - должен быть массив с минимум 1 элементом

3. **Проверьте формат телефона:**
    - ✅ Правильно: `+79991234567`
    - ❌ Неправильно: `89991234567`, `79991234567`, `+7 999 123 45 67`

---

**Пример правильного JSON:**
```json
{
  "customerName": "Имя Фамилия",
  "customerPhone": "+79991234567",
  "items": [
    {
      "pizzaId": 1,
      "quantity": 2,
      "price": 450.00
    }
  ]
}
```

---

### ⚠️ Ошибка 3: 404 Not Found

**Что означает:**
Запрошенный ресурс не найден.

---

**Причины:**
- ❌ Пицца с указанным `pizzaId` не существует в БД
- ❌ Заказ с указанным `id` не найден
- ❌ Неправильный URL эндпоинта (опечатка)
- ❌ Заказ был удалён (soft delete)

---

**Решение:**

1. **Проверьте существующие ID:**
```bash
   # Получить список пицц с ID
   GET http://localhost:8080/api/pizzas
   
   # Получить список заказов с ID
   GET http://localhost:8080/api/orders
```

2. **Используйте ID из полученного списка**

3. **Проверьте правильность URL:**
    - ✅ Правильно: `/api/orders/1`
    - ❌ Неправильно: `/api/order/1`, `/api/orders/id/1`

---

### ⚠️ Ошибка 4: 500 Internal Server Error

**Что означает:**
Внутренняя ошибка сервера (ошибка в коде или проблема с БД).

---

**Причины:**
- ❌ Приложение не запущено
- ❌ PostgreSQL не работает
- ❌ Нет соединения с БД
- ❌ Ошибка в миграциях Flyway
- ❌ Баг в коде приложения

---

**Решение:**

**Шаг 1: Проверить, что контейнеры запущены:**
```bash
docker ps
```

**Должны работать:**
- `pizzeria-app`
- `pizzeria-db`

---

**Шаг 2: Проверить логи приложения:**
```bash
docker logs pizzeria-app
```

**Искать строки с ERROR:**
- Если видите "Connection refused" → проблема с PostgreSQL
- Если видите "Flyway" → проблема с миграциями
- Другие ошибки → скопируйте и поищите решение

---

**Шаг 3: Проверить логи БД:**
```bash
docker logs pizzeria-db
```

---

**Шаг 4: Перезапустить всё:**
```bash
docker-compose down
docker-compose up --build
```

Подождите, пока появится:
```
Started PizzeriaApplication in X seconds
```

Потом повторите запрос.

---

### ⚠️ Ошибка 5: Connection refused / ERR_CONNECTION_REFUSED

**Что означает:**
Невозможно подключиться к серверу.

---

**Причины:**
- ❌ Приложение не запущено
- ❌ Неправильный порт (должен быть 8080)
- ❌ Приложение запускается, но ещё не готово

---

**Решение:**

1. **Проверить статус:**
```bash
   docker ps
```

2. **Проверить логи:**
```bash
   docker logs pizzeria-app
```

3. **Подождать запуска:**
    - После `docker-compose up` нужно подождать 30-60 секунд
    - Ищите в логах: `Started PizzeriaApplication`

4. **Проверить доступность:**
    - Откройте в браузере: `http://localhost:8080/`
    - Должна открыться главная страница

---

## ✅ Чек-лист успешного тестирования

### Postman:

- ☑️ Коллекция создана: `Pizzeria (finalProject)`
- ☑️ Авторизация настроена: `user` / `user123`
- ☑️ GET /api/pizzas → 200 OK, 4 пиццы
- ☑️ POST /api/orders → 201 Created
- ☑️ totalPrice рассчитан правильно
- ☑️ GET /api/orders → 200 OK, список заказов
- ☑️ GET /api/orders/{id} → 200 OK, один заказ
- ☑️ PATCH /status → 200 OK, статус изменён
- ☑️ Фильтры работают (?status=NEW)
- ☑️ DELETE → 204 No Content

---

### Swagger UI:

- ☑️ Страница открывается: `http://localhost:8080/swagger-ui/index.html`
- ☑️ Авторизация выполнена: кнопка "Authorize"
- ☑️ Все эндпоинты видны и документированы
- ☑️ Try it out работает
- ☑️ POST /api/orders → 201 Created
- ☑️ GET /api/orders → 200 OK
- ☑️ Schemas отображаются внизу

---

**Если все пункты ✅ - тестирование пройдено успешно!** 🎉

---
## 📚 Дополнительные ресурсы

- **Postman Learning Center:** https://learning.postman.com/
- **Swagger/OpenAPI Specification:** https://swagger.io/specification/
- **JSON Validator:** https://jsonlint.com/
- **HTTP Status Codes:** https://httpstatuses.com/
- **Basic Auth explained:** https://developer.mozilla.org/en-US/docs/Web/HTTP/Authentication

---

## 🎯 Учётные данные (сводка)

**Для быстрого доступа:**
```
Username: user
Password: user123

Альтернативный:
Username: admin
Password: admin123
```

**URL приложения:** `http://localhost:8080`

**Swagger UI:** `http://localhost:8080/swagger-ui/index.html`

---